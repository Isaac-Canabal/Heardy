# Dependencias para correr la suite de tests (pytest). Deliberadamente NO
# incluye requirements.txt entero (yt-dlp, el proveedor de PO tokens): los
# tests son de lógica pura (auth.py, config.py, rate_limit.py) y no llaman a
# ytdlp_client, así que no hace falta esa pieza pesada solo para testear.
# Si algún día se añaden tests que sí importan app.main, esto debería pasar
# a "-r requirements.txt".
fastapi==0.115.6
python-dotenv==1.0.1
pytest==8.3.4
pytest-asyncio==0.25.2
# test_firebase_auth.py firma tokens de prueba con una clave RSA propia
# (nunca contra Google) — necesita PyJWT[crypto], igual que el runtime.
PyJWT[crypto]==2.10.1
